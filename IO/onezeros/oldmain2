#include<queue>
#include<iostream>
#include<vector>

using namespace std;

struct info{
    int rest;
    int level;
    vector<int> nums;
}


int main(){
    int ncases;
    cin >> ncases;

    for(int i = 0; i < ncases; n++){
        int curnum;

        queue<info> sq;
        int index = 0;

        rests[0] = 1 % curnum;
        for(int i = 1; i < 100; i ++){
            rests[i] = (rests[i - 1] * 10) % curnum ;
        }

        while(!sq.empty()){
            push
        }

    }

}
